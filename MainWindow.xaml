<Window x:Class="SimpleInventoryApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleInventoryApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:ProductViewModel/>
    </Window.DataContext>

    <DockPanel  Margin="10">

        <Grid DockPanel.Dock="Top" Width="200" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search Icon -->
            <TextBlock Grid.Column="0" Text="🔍" FontSize="14" VerticalAlignment="Center" Margin="8,0,5,0" Foreground="Gray"/>

            <!-- Search TextBox -->
            <TextBox x:Name="SearchBox" Grid.Column="1" 
             Text="{Binding ProductSearchQuery, UpdateSourceTrigger=PropertyChanged}" 
             BorderThickness="0" 
             VerticalContentAlignment="Center"
             Padding="0,5"/>

            <!-- Clear Button -->
            <Button x:Name="ClearButton" Grid.Column="2" Content="✕" 
            Width="20" Height="20" 
            Margin="5,0,5,0"
            BorderThickness="0" 
            Background="Transparent" 
            Foreground="Gray"
            Cursor="Hand"
            FontSize="12"
            Visibility="Collapsed"
            Command = "{Binding ClearSearchCommand}"/>

            <!-- Placeholder -->
            <TextBlock Grid.Column="1" IsHitTestVisible="False" 
               Text="Search products..." 
               Foreground="LightGray" 
               VerticalAlignment="Center"
               Margin="0,0,25,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=SearchBox, Path=Text.Length}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Border around the whole search box -->
            <Border Grid.ColumnSpan="3" BorderBrush="LightGray" BorderThickness="1" CornerRadius="3" IsHitTestVisible="False"/>
        </Grid>

        <DataGrid Height="250" ItemsSource="{Binding ProductView}"
          DockPanel.Dock="Top"
          Width="400"
          SelectedItem="{Binding SelectedProduct}" 
          AutoGenerateColumns="False"
          CanUserAddRows="False" 
          Margin="0,0,0,10"
          AlternatingRowBackground="#F5F5F5"
          RowBackground="White"
          GridLinesVisibility="All"
          HeadersVisibility="Column"
          HorizontalGridLinesBrush="#E0E0E0"
          VerticalGridLinesBrush="#E0E0E0"        
          BorderBrush="LightGray"
          BorderThickness="1"
          CanUserResizeRows="False"
          SelectionMode="Single"
          >

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#E8E8E8"/>
                    <Setter Property="Foreground" Value="#333"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="LightGray"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E3F2FD"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="35"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F0F0F0"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E3F2FD"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding ProductName}" Width="2*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding ProductCategory}" Width="1.5*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding ProductQuantity}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Margin" Value="0,0,10,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel DockPanel.Dock="Bottom" Width="750" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,10">

            <!-- Input Fields Section -->
            <Border BorderBrush="#D0D0D0" BorderThickness="1" Width="450" Height="60" CornerRadius="4" Padding="15,10" Margin="0,0,15,0" Background="White">
                <StackPanel Orientation="Horizontal">
                    <Grid Margin="0,0,10,0">
                        <TextBox Width="150" Height="32" Padding="8,6" Text="{Binding ProductName}" x:Name="ProductName"
                         BorderBrush="#BDBDBD" BorderThickness="1" VerticalContentAlignment="Center"/>
                        <TextBlock IsHitTestVisible="False" Margin="10,0,0,0" Text="Product Name" Foreground="#999" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=ProductName, Path=Text.Length}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <Grid Margin="0,0,10,0">
                        <TextBox Width="120" Height="32" Padding="8,6" Text="{Binding ProductCategory}" x:Name="ProductCategory"
                         BorderBrush="#BDBDBD" BorderThickness="1" VerticalContentAlignment="Center"/>
                        <TextBlock IsHitTestVisible="False" Margin="10,0,0,0" Text="Category" Foreground="#999" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=ProductCategory, Path=Text.Length}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <Grid>
                        <TextBox Width="80" Height="32" Padding="8,6" Text="{Binding ProductQuantity}" x:Name="ProductQuantity"
                         BorderBrush="#BDBDBD" BorderThickness="1" VerticalContentAlignment="Center"/>
                        <TextBlock IsHitTestVisible="False" Margin="10,0,0,0" Text="Quantity" Foreground="#999" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=ProductQuantity, Path=Text.Length}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Action Buttons Section -->
            <StackPanel Orientation="Horizontal">
                <Button Content="Add" Width="70" Height="32" Margin="0,0,8,0" Command="{Binding AddCommand}"
                Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="SemiBold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="4" 
                                        BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#45A049"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#3D8B40"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Edit" Width="70" Height="32" Margin="0,0,8,0" Command="{Binding EditCommand}"
                Background="#2196F3" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="SemiBold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="4" 
                                        BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1976D2"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#1565C0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Delete" Width="70" Height="32" Command="{Binding DeleteCommand}"
                Background="#F44336" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="SemiBold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="4" 
                                        BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#D32F2F"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#C62828"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

        </StackPanel>
    </DockPanel>

    
</Window>
